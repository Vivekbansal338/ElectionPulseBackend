// routes/mediaChannelRoutes.js
import express from "express";
// import {
//   createMediaChannel,
//   getAllMediaChannels,
//   getMediaChannelById,
//   updateMediaChannel,
//   deleteMediaChannel,
// } from "../controllers/mediaChannelControllers.js";

const router = express.Router();

// Create a new media channel
// router.post("/", createMediaChannel);

// Get all media channels
// router.get("/", getAllMediaChannels);

// Get media channel by ID
// router.get("/:id", getMediaChannelById);

// Update media channel by ID
// router.put("/:id", updateMediaChannel);

// Delete media channel by ID
// router.delete("/:id", deleteMediaChannel);

export default router;
